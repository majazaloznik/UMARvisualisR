% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/double_y_axes.R
\name{axis_on_grid}
\alias{axis_on_grid}
\title{One axis on a nice grid with exactly n intervals}
\usage{
axis_on_grid(lo, hi, n, j = NULL, ref = 0)
}
\arguments{
\item{lo, hi}{data range to cover}

\item{n}{number of intervals (there are n + 1 breaks)}

\item{j}{intervals below zero, or NULL to let the range float}

\item{ref}{reference line (usually 0 or 100)}
}
\value{
list(ylim, y_breaks, step), or NULL if no nice step fits
}
\description{
With \code{j} supplied the axis is anchored so that zero falls exactly on
the j-th break, which is what lets two axes share a zero line. With
\code{j = NULL} the range floats to whatever covers the data most tightly,
padded up to n intervals — never pushing an all-positive axis below zero.
}
\keyword{internal}
