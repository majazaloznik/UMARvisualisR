% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/double_y_axes.R
\name{pair_y_scales}
\alias{pair_y_scales}
\title{Two y scales that share their gridlines}
\usage{
pair_y_scales(
  values_left,
  values_right,
  ylim = NULL,
  ylim2 = NULL,
  ref = 0,
  ref2 = 0,
  n_range = 3:7
)
}
\arguments{
\item{values_left, values_right}{numeric vectors of the data on each axis}

\item{ylim, ylim2}{numeric(2) manual limits, or NULL to derive}

\item{ref}{reference (usually 0 or 100)}

\item{ref2}{reference (usually 0 or 100)}

\item{n_range}{interval counts to consider}
}
\value{
list(left, right, problems), each side shaped like
  \link{find_pretty_ylim} (ylim, y_breaks)
}
\description{
Returns one scale per axis with the same number of intervals, so the
horizontal gridlines serve both. When both sides straddle zero, the zero
line is forced onto the same gridline. Either side can be fixed with
\code{ylim}/\code{ylim2}; the other is then derived to match it.
}
