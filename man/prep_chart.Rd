% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_prep_chart.R
\name{prep_chart}
\alias{prep_chart}
\title{Prepare a chart configuration from a dataframe}
\usage{
prep_chart(
  data,
  type = "line",
  title = NULL,
  y_axis = NULL,
  legend = NULL,
  colours = NULL,
  dashed = NULL,
  dotted = NULL,
  xmin = NULL,
  xmax = NULL,
  stacked = FALSE,
  emphasis = NULL,
  legend_columns = 2,
  rolling = NULL,
  growth = NULL,
  index = NULL,
  ylim = NULL,
  note = NULL
)
}
\arguments{
\item{data}{dataframe in wide or long format}

\item{type}{character, either a single value applied to all series or a vector
of per-series values. Valid: "line", "bar" or "area" (for max 2 series). Defaults to "line".}

\item{title}{character, chart title. Defaults to NULL (no title).}

\item{y_axis}{character, y-axis label. Defaults to NULL.}

\item{legend}{character vector of legend labels. Defaults to column names (wide)
or unique group values (long).}

\item{colours}{integer vector (indices into UMAR palette) or character vector
(hex colours). Defaults to sequential palette colours.}

\item{dashed}{integer vector for which series should be drawn dashed}

\item{dotted}{integer vector for which series should be drawn dotted}

\item{xmin}{Date or character coercible to Date. Left x-axis limit.}

\item{xmax}{Date or character coercible to Date. Right x-axis limit.}

\item{stacked}{logical, stack bar series. Defaults to FALSE.}

\item{emphasis}{numeric value(s) for emphasised horizontal gridlines.
NULL (default) auto-detects: 0 if in range, 100 if y_axis contains
"indeks"/"index". FALSE disables. Numeric vector for explicit values.}

\item{legend_columns}{integer, number of columns in the legend. Defaults to 2.}

\item{rolling}{number of periods to calculate rolling mean over}

\item{growth}{"YOY", "MOM" or "QOQ"}

\item{index}{base_period either a year in YYYY format or quarter in 2023Q2 format
or month in 2023M03 format}

\item{ylim}{numeric(2) manual y-axis limits, e.g. c(90, 120).
NULL (default) auto-detects. Ignored for bar charts (must include 0).}

\item{note}{character string, possibly with \\n breaks}
}
\value{
An object of class "umar_chart" containing the data and config.
}
\description{
Takes a dataframe in wide or long format and prepares a chart configuration
object that can be passed to \link[UMARvisualisR]{view_chart},
\link[UMARvisualisR]{save_chart}, or \link[UMARvisualisR]{get_code}.
}
\details{
Wide format: first column is date (Date class), remaining columns are numeric
series. Column names become legend labels by default.

Long format: exactly three columns — one Date, one numeric (values), one
character or factor (series names) or multiple character or factor which get
concatented into series names unless legend in given.
}
